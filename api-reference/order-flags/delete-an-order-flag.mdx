---
title: "Delete an order flag"
description: "Permanently deletes the order flag whose `id` you pass as `order_flag_id`. Returns no body."
openapi: "DELETE /admin/order_flags/{order_flag_id}"
---

Deletes the order flag permanently and returns `204` with no body. The flag no longer appears in the listing, and the listing's `pagination.total` goes down by one. Retrieving it or deleting it again returns `404`.

<Note>The SDK samples get their client from credentials your app registers once when it starts, as shown in [Authentication](/authentication#set-up-a-client). To try the call here, click **Try it**, set `store` to your store's host name and paste an access token from [Get an access token](/api-reference/authentication/get-an-access-token).</Note>
