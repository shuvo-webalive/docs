---
title: "Delete a redirect"
description: "Deletes a redirect permanently and returns `204` with no body."
openapi: "DELETE /admin/settings/seo_configuration/redirects/{redirect_id}"
---

Deletes the redirect and returns `204` with no body. After that, [Retrieve a redirect](/api-reference/seo-configuration/retrieve-a-redirect) returns `404` for its `redirect_id`. No call restores a deleted redirect.

<Note>The SDK samples get their client from credentials your app registers once when it starts, as shown in [Authentication](/authentication#set-up-a-client). To try the call here, click **Try it**, set `store` to your store's host name and paste an access token from [Get an access token](/api-reference/authentication/get-an-access-token).</Note>
