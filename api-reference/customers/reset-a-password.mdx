---
title: "Reset a password"
description: "Set a new password without the old one."
openapi: "POST /admin/customers/{customer_id}/reset-password"
---

Sets the customer's password. The old password is not needed.

<Note>Every SDK sample assumes a client set up once, as shown on the [Customers overview](./overview).</Note>
