---
title: "Update a shopping cart section"
description: "Changes the fields you send in one section and returns that section."
openapi: "PATCH /admin/settings/page_settings/shopping_cart/{section}"
---

Send the fields to change, wrapped in the section's own name, such as `cart_page`. That is the only wrapper accepted: a body wrapped in `shopping_cart`, or not wrapped at all, is rejected with `400 invalid_request`. Returns the whole section after the change, under the same key and without `group_meta`.

<Note>The SDK samples get their client from credentials your app registers once when it starts, as shown in [Authentication](/authentication#set-up-a-client). To try the call here, click **Try it**, set `store` to your store's host name and paste an access token from [Get an access token](/api-reference/authentication/get-an-access-token). To send a raw JSON body instead of filling in fields, copy the cURL sample and edit its `-d` payload.</Note>
