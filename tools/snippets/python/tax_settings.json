{
  "sdk": "python",
  "module": "tax_settings",
  "endpoints": {
    "get_tax_settings": {
      "function": "get_tax_settings",
      "code": "response = client.tax_settings.get_tax_settings()\nsettings = response.map[\"tax_settings\"]\nprint(settings[\"default_country\"], settings[\"default_tax_code\"])\nprint(settings[\"default_tax_profile\"], settings[\"show_price_with_tax\"])\n"
    },
    "put_tax_settings": {
      "function": "put_tax_settings",
      "code": "response = client.tax_settings.put_tax_settings({\n    \"settings\": {\"tax_message\": \"All prices include GST\"},\n})\nprint(response.map[\"tax_settings\"][\"tax_message\"])\n"
    }
  }
}
