---
title: "Customers"
sidebarTitle: "Overview"
description: "Read, create, update and remove customers, and manage their addresses, passwords and store credit."
---

The Customers API has fifteen endpoints, and you can call every one from all seven SDKs. Every path starts with `/api/v4/admin/customers`.

<Warning>
  Paths take the customer's `customer_id`. Each customer also has an `internal_id`, a separate
  number that no path takes. If you send an `internal_id` by mistake and it matches another
  customer's `customer_id`, you get **that other customer with a `200`** instead of a `404`.
  Check that the customer you get back is the one you asked for.
</Warning>

## At a glance

| Endpoint | Method | Path | What it does |
| --- | --- | --- | --- |
| [List customers](/api-reference/customers/list-customers) | <span style={{fontFamily: "var(--font-mono, monospace)", fontSize: "0.72rem", fontWeight: 700, letterSpacing: "0.02em", color: "#0f7b6c"}}>GET</span> | `…/customers` | Returns up to 20 customers per call, with the total count and the next page's URL. |
| [Retrieve a customer](/api-reference/customers/retrieve-a-customer) | <span style={{fontFamily: "var(--font-mono, monospace)", fontSize: "0.72rem", fontWeight: 700, letterSpacing: "0.02em", color: "#0f7b6c"}}>GET</span> | `…/customers/{customer_id}` | Returns one customer, with its addresses and custom properties, by `customer_id`. |
| [Count customers](/api-reference/customers/count-customers) | <span style={{fontFamily: "var(--font-mono, monospace)", fontSize: "0.72rem", fontWeight: 700, letterSpacing: "0.02em", color: "#0f7b6c"}}>GET</span> | `…/customers/count` | Returns the number of customers, or the number that match the filters you send. |
| [Check an email address](/api-reference/customers/check-an-email-address) | <span style={{fontFamily: "var(--font-mono, monospace)", fontSize: "0.72rem", fontWeight: 700, letterSpacing: "0.02em", color: "#0f7b6c"}}>GET</span> | `…/customers/check-email` | Checks whether an email address already belongs to a customer, and returns that customer's ids. |
| [Export customers](/api-reference/customers/export-customers) | <span style={{fontFamily: "var(--font-mono, monospace)", fontSize: "0.72rem", fontWeight: 700, letterSpacing: "0.02em", color: "#0f7b6c"}}>GET</span> | `…/customers/export` | Returns every customer except deleted ones as an XLSX spreadsheet; parameters choose the columns. |
| [Create a customer](/api-reference/customers/create-a-customer) | <span style={{fontFamily: "var(--font-mono, monospace)", fontSize: "0.72rem", fontWeight: 700, letterSpacing: "0.02em", color: "#2563eb"}}>POST</span> | `…/customers` | Creates a customer and returns it, including the `customer_id` you use in later calls. |
| [Update a customer](/api-reference/customers/update-a-customer) | <span style={{fontFamily: "var(--font-mono, monospace)", fontSize: "0.72rem", fontWeight: 700, letterSpacing: "0.02em", color: "#b45309"}}>PUT</span> | `…/customers/{customer_id}` | Changes only the fields you send and returns the updated customer. |
| [Delete a customer](/api-reference/customers/delete-a-customer) | <span style={{fontFamily: "var(--font-mono, monospace)", fontSize: "0.72rem", fontWeight: 700, letterSpacing: "0.02em", color: "#c2410c"}}>DELETE</span> | `…/customers/{customer_id}` | Moves a customer to the trash; the API cannot restore it. |
| [Add addresses](/api-reference/customers/add-addresses) | <span style={{fontFamily: "var(--font-mono, monospace)", fontSize: "0.72rem", fontWeight: 700, letterSpacing: "0.02em", color: "#2563eb"}}>POST</span> | `…/customers/{customer_id}/addresses` | Adds primary, shipping or billing addresses to a customer and returns the updated customer. |
| [Reset a password](/api-reference/customers/reset-a-password) | <span style={{fontFamily: "var(--font-mono, monospace)", fontSize: "0.72rem", fontWeight: 700, letterSpacing: "0.02em", color: "#2563eb"}}>POST</span> | `…/customers/{customer_id}/reset-password` | Sets a new password for a customer without needing their current one. |
| [Change a password](/api-reference/customers/change-a-password) | <span style={{fontFamily: "var(--font-mono, monospace)", fontSize: "0.72rem", fontWeight: 700, letterSpacing: "0.02em", color: "#2563eb"}}>POST</span> | `…/customers/{customer_id}/change-password` | Changes a customer's password after checking their current password. |
| [Bulk reset passwords](/api-reference/customers/bulk-reset-passwords) | <span style={{fontFamily: "var(--font-mono, monospace)", fontSize: "0.72rem", fontWeight: 700, letterSpacing: "0.02em", color: "#2563eb"}}>POST</span> | `…/customers/bulk-reset-password` | Queues a password reset email for each customer id you send in `customer_ids`. |
| [Import customers](/api-reference/customers/import-customers) | <span style={{fontFamily: "var(--font-mono, monospace)", fontSize: "0.72rem", fontWeight: 700, letterSpacing: "0.02em", color: "#2563eb"}}>POST</span> | `…/customers/import` | Creates or updates customers from an uploaded CSV, XLS or XLSX file. |
| [List store credit adjustments](/api-reference/customers/list-store-credit-adjustments) | <span style={{fontFamily: "var(--font-mono, monospace)", fontSize: "0.72rem", fontWeight: 700, letterSpacing: "0.02em", color: "#0f7b6c"}}>GET</span> | `…/customers/{customer_id}/store-credit/adjustments` | Returns a customer's store credit adjustments, newest first, up to 20 at a time. |
| [Adjust store credit](/api-reference/customers/adjust-store-credit) | <span style={{fontFamily: "var(--font-mono, monospace)", fontSize: "0.72rem", fontWeight: 700, letterSpacing: "0.02em", color: "#2563eb"}}>POST</span> | `…/customers/{customer_id}/store-credit/adjustments` | Adds to or deducts from a customer's store credit and returns the new balance. |

## Before you call

Set up a client once with your store's credentials, as shown in [Authentication](/authentication).
The SDK samples on these pages use that client. With cURL, send the access token in an
`Authorization` header: `Authorization: Bearer $ACCESS_TOKEN`.

## Things to know

- **A listing returns at most 20 records per call.** A larger `limit` is not rejected, but you
  still get at most 20, and `pagination.limit` shows `20`. To get more, send the next `page`
  number or a higher `offset` for as long as `pagination.has_next` is `true`.
- **A new customer cannot sign in until it is active.** If you create a customer without
  `status: "active"`, it is created as `awaiting_verification` with no usable login, even if you
  send a `password`, and changing its password fails with `401`. Send `status: "active"` with a
  `password` to create a customer who can sign in.
- **An export always contains every customer.** Its parameters choose columns, not customers: a
  column is removed when its parameter is anything other than `1`, `true`, `on` or `yes`, so
  `status=active` exports everyone and removes the `Status` column. Importing an export rewrites
  every customer in the store, so build import files from only the customers you want to change.
- **Every error response has the same fields:** `status`, `code` and `message`, plus `error` (a
  machine-readable reason) and `errors` (one entry per rejected field) when the API provides them.
